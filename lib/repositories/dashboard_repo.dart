// import 'package:kipgo/models/dashboard_stats.dart';

// class DashboardRepository {
//   Future<DashboardStats> getStats();

//   final totalBookings = bookings.length;

//   final activeBookings = bookings.where((b) => b.status == 'ongoing').length;

//   final upcomingBookings = bookings
//       .where((b) => b.status == 'approved' || b.status == 'reserved')
//       .length;

//   final awaitingVerification = bookings
//       .where((b) => b.payment?.status == 'awaiting_verification')
//       .length;
// }
